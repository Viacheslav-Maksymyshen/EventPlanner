import React from "react";

const EventCard = () => {
  // Відображення інформації про подію (наприклад, відображення одного елемента списку)
  return <div>Event Card</div>;
};

export default EventCard;
